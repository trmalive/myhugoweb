{
  "name": "hugo-site",
  "version": "1.0.0",
  "description": "Hugo site with embedded tools",
  "engines": {
    "node": "24.x"
  },
  "packageManager": "npm@10.8.2",
  "scripts": {
    "build:editor": "echo 'Skipping tool build, using pre-built assets'",
    "deploy:editor": "echo 'Skipping tool deploy, assets already in static/tools/md'",
    "build": "npm run build:editor && npm run deploy:editor && hugo --gc --minify --buildFuture"
  },
  "devDependencies": {}
}
