{
  "name": "hugo-site",
  "version": "1.0.0",
  "description": "Hugo site with embedded tools",
  "engines": {
    "node": "24.x"
  },
  "packageManager": "pnpm@10.4.0",
  "scripts": {
    "build:editor": "cd submodules/md && pnpm install --no-frozen-lockfile --registry=https://registry.npmjs.org/ && pnpm --filter @md/web run build",
    "deploy:editor": "mkdir -p static/tools/md && cp -r submodules/md/apps/web/dist/* static/tools/md/",
    "build": "npm run build:editor && npm run deploy:editor && hugo --gc --minify --buildFuture"
  },
  "devDependencies": {}
}
