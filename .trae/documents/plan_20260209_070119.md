收到！我们将明确指定 Vercel 使用 **Hugo v0.146.0** 版本。

### 执行计划

1. **修改** **`vercel.json`**：
   在配置中显式添加环境变量 `HUGO_VERSION` 为 `0.146.0`。

   ```json
   {
     "framework": "hugo",
     "buildCommand": "hugo --gc --minify",
     "outputDirectory": "public",
     "build": {
       "env": {
         "HUGO_VERSION": "0.146.0"
       }
     }
   }
   ```
2. **推送更新**：
   提交更改至 GitHub。

这将强制 Vercel 下载并使用 0.146.0 版本进行构建，完美支持 \`hugo
