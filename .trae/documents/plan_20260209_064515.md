明白了！这是一个非常专业的选择，保留了 Hugo 原生的定时发布能力。

既然如此，我们就不修改全局配置了（甚至应该撤销刚才添加的 `buildFuture = true`），而是通过**调整这篇具体文章的时间**来解决显示问题。

### 计划步骤：
1.  **撤销全局配置**：
    从 `hugo.toml` 中删除刚才添加的 `buildFuture = true`，恢复默认的“定时发布”机制。

2.  **修改文章时间**：
    将 `content/posts/teaching-research-management.md` 中的 `date` 修改为昨天（例如 `2026-02-08`）。

3.  **推送更新**：
    提交这两个修改到 GitHub。

这样，这篇“时间已过去”的文章会立即显示，而未来您设定的“将来时”文章则会等待时间到达后（配合再次构建）才显示。

请确认执行。
